
name: "Pelican InElegant Build"
description: "Build a static site using Pelican InElegant"
author: "Christopher O'Brien  <obriencj@gmail.com>"


inputs:
  version:
    description: "pelican-inelegant container version"
    required: true
    default: "master"

  config:
    description: "pelican config file"
    required: true
    default: "pelicanconf.py"


runs:
  using: composite
  steps:
    - name: Running inelegant container
      uses: docker://ghcr.io/obriencj/pelican-inelegant:${{ inputs.version }}
      with:
        args: --settings "${{ inputs.config }}"


# The end.
